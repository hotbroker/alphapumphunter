[project]
name = "alphapumphunter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "curl-cffi>=0.14.0",
    "httpx[socks]>=0.25",
    "loguru>=0.7",
    "pybit>=5.10",
]
